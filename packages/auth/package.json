{
  "name": "@astrolegia/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Configuración de Better Auth (Google SSO) montada en la API unificada. Se consume desde su fuente TypeScript: apps/api la empaqueta en su build.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@astrolegia/database": "workspace:*",
    "better-auth": "^1.7.3"
  },
  "devDependencies": {
    "@astrolegia/tsconfig": "workspace:*",
    "@types/node": "^22.13.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
